/** @deprecated Import from `@/lib/security/external-content` — re-export for Strata compatibility. */
export {
  UNTRUSTED_EXTERNAL_CONTENT_GUARDRAIL,
  buildPromptSafeRawInputBlock,
  sanitizeRawUserInput,
  sanitizeUntrustedText,
  wrapUntrustedContent,
} from "@/lib/security/external-content/untrusted";
