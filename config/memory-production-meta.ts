/**
 * Facts about production memory retrieval (Qdrant + Mem0 embedder).
 * Safe to import from client components; keep in sync with `mem0-config.ts` usage.
 */
export const MEMORY_PRODUCTION_QDRANT_COLLECTION = "memories_v2";
export const MEMORY_PRODUCTION_EMBEDDER_MODEL = "nomic-embed-text";
export const MEMORY_PRODUCTION_EMBEDDING_DIMS = 768;
