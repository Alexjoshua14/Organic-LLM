/**
 * Facts about production memory retrieval (Qdrant + Mem0 embedder).
 * Safe to import from client components; keep in sync with `mem0-config.ts` usage.
 */
export const MEMORY_PRODUCTION_QDRANT_COLLECTION = "memories";
export const MEMORY_PRODUCTION_EMBEDDER_MODEL = "all-minilm";
export const MEMORY_PRODUCTION_EMBEDDING_DIMS = 384;
