/** Tiny lightweight "beta" marker for in-development Stratum surfaces. */
export function StratumBetaBadge() {
  return (
    <span className="text-[9px] font-light lowercase tracking-[0.2em] text-muted-foreground/60">
      beta
    </span>
  );
}
